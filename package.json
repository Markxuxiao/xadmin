{
  "name": "xadmin",
  "version": "0.1.0",
  "private": true,
  "description": "XAdmin 通用后台管理系统框架",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "backend:dev": "ts-node --esm --transpile-only apps/project-alpha/backend/main.ts"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  },
  "dependencies": {
    "@nestjs/common": "^10.3.0",
    "@nestjs/core": "^10.3.0",
    "@nestjs/platform-express": "^10.3.0",
    "reflect-metadata": "^0.2.0"
  },
  "devDependencies": {
    "ts-node": "^10.9.2"
  }
}
